type User {
    id: ID
    username: String
}

type Auth {
    accessToken: String!
}

type Mutation {
    signUp(username: String!, password: String!): User
    signIn(username: String!, password: String!): Auth
}
